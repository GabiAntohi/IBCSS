#!/usr/bin/env node

/**
 * Module dependencies.
 */
var config = require('../config/config.js');
require('../app')(config)
    .then(null, function (err) {
        console.log(err)
        process.exit(1)
    });
